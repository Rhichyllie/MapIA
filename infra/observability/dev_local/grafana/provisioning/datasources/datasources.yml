apiVersion: 1

datasources:
  - name: Prometheus
    uid: PROM_DS_DEV_LOCAL
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Loki
    uid: LOKI_DS_DEV_LOCAL
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
